__pycache__/
*.pyc
.venv/
.DS_Store

# Round 4 (reports/04-historical.md): the maintainer's own broader research data (household
# survey microdata, unrelated Stata .do/.dta files, draft paper output), placed locally to
# supply the 2023 audit CSVs this project needed. Only hm-ds/Data/audit1.csv and audit2.csv
# are actually ingested (as an archived artifact under archive/manual-audit-2023/); everything
# else here is out of scope for this project and must never be committed -- it's survey
# microdata with no established basis for redistribution under this project's own public,
# append-only archive.
hm-ds/

# Round 6 (reports/06-live.md): outreach-emails.md named a real individual alongside internal
# negotiating strategy -- content with ongoing value to the maintainer, but that must never be
# in a public repo. Removed from history (git-filter-repo) before the first push. docs/private/
# is a standing location for anything else of this kind found later.
docs/outreach-emails.md
docs/private/
